# =============================================================================
# Nexora University – UniSphere AI Backend Dependencies
# Vercel-compatible: no local ML models, uses HuggingFace Inference API
# =============================================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Configuration ---
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# --- HTTP Client (used for HF Inference API + Supabase) ---
httpx==0.28.1

# --- Database & Storage (Supabase) ---
supabase==2.11.0

# --- Vector Database (Pinecone) ---
pinecone-client==5.0.1

# --- LLM Provider (Groq) ---
langchain-groq==0.2.3
langchain==0.3.14
langchain-community==0.3.14

# --- Document Processing ---
pymupdf==1.25.3

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Email Validation ---
email-validator==2.2.0
