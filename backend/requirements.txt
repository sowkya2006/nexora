# =============================================================================
# Nexora University – UniSphere AI Backend Dependencies
# =============================================================================

# --- Web Framework ---
# FastAPI: Modern async Python web framework for building REST APIs
fastapi==0.115.6

# Uvicorn: ASGI server that runs the FastAPI application
uvicorn[standard]==0.34.0

# Python-multipart: Required for file upload endpoints (document PDF uploads)
python-multipart==0.0.20

# --- Configuration ---
# Python-dotenv: Loads environment variables from .env files
python-dotenv==1.0.1

# Pydantic: Data validation and settings management via Python type hints
pydantic==2.10.4

# Pydantic-settings: Settings class that reads from environment variables
pydantic-settings==2.7.0

# --- HTTP Client ---
# HTTPX: Async HTTP client used internally by Supabase SDK and for external calls
httpx==0.28.1

# --- Database & Storage (Supabase) ---
# Supabase: Official Python client for Supabase PostgreSQL, Auth, and Storage
supabase==2.11.0

# --- Vector Database (Pinecone) ---
# Pinecone-client: SDK for Pinecone vector database (RAG document retrieval)
pinecone-client==5.0.1

# --- LLM Provider (Groq) ---
# LangChain-Groq: LangChain integration for Groq API (Llama 3.3 70B Instruct)
langchain-groq==0.2.3

# --- AI / RAG Framework (LangChain) ---
# LangChain: Orchestrates RAG pipeline (retrieval, prompts, LLM chains)
langchain==0.3.14

# LangChain-community: Community integrations (document loaders, utilities)
langchain-community==0.3.14

# LangChain-huggingface: HuggingFace embedding model integration (bge-large-en-v1.5)
langchain-huggingface==0.1.2

# --- Embeddings ---
# Sentence-transformers: Runs BAAI/bge-large-en-v1.5 embedding model locally
sentence-transformers==3.3.1

# --- Document Processing ---
# PyMuPDF (fitz): Extracts text from PDF documents for knowledge base indexing
pymupdf==1.25.3

# --- Authentication ---
# Python-jose: JWT token encoding/decoding for admin session validation
python-jose[cryptography]==3.3.0

# Passlib: Password hashing utilities (used with Supabase Auth helpers)
passlib[bcrypt]==1.7.4

# --- Email Validation ---
# Email-validator: Required by Pydantic EmailStr fields in auth schemas
email-validator==2.2.0
